# billing plugin — Copilot CLI only

This plugin reads GitHub Copilot CLI session data (`session.shutdown` events from
`$COPILOT_HOME/session-state/*/events.jsonl`) and is not supported in Gemini CLI.
